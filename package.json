{
  "name": "dokusho-kiroku-app",
  "version": "1.0.0",
  "private": true,
  "description": "読書記録アプリ",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test"
  },
  "dependencies": {
    "stripe": "^22.6.0"
  }
}
